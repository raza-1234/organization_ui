//naming convrntion

export enum ButtonVariant {
  PRIMARY = "primary",
  SECONDARY = "secondary"
}

export enum ToastTimeout {
  LONG = 6000,
  STANDARD = 4000,
  SHORT = 3000
}

export enum ToastVariant {
  SUCCESS = 'success',
  ERROR =  'error'
}