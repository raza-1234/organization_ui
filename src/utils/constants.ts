export enum ButtonVariant {
  PRIMARY = "primary",
  SECONDARY = "secondary"
}

export type ButtonType = "submit" | "reset" | "button";
export const DefaultButtonType = "button";

export enum ToastTimeout {
  LONG = 6000,
  STANDARD = 4000,
  SHORT = 3000
}

export enum ToastVariant {
  SUCCESS = 'success',
  ERROR =  'error'
}

export enum ModalButtonText {
  OK = "ok",
  CANCEL = "cancel"
}

export enum TooltipPosition {
  TOP = "top",
  BOTTOM = "bottom"
}

export enum ShapePosition {
  TOP = 'shape_top',
  BOTTOM = 'shape_bottom'
}

export const MODAL_TITLE = "modal title text";
export const INPUT_TYPE = "text"; 
export const TOOLTIP = "Tooltip";

